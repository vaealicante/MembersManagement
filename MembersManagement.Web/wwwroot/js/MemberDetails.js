document.addEventListener("DOMContentLoaded", function () {
    console.log("Member Details View Loaded.");

    // Example: Highlight the return button if the user has been idle
    const returnBtn = document.querySelector('.btn-outline-secondary');
    if (returnBtn) {
        // You can add any view-specific JS logic here
    }
});